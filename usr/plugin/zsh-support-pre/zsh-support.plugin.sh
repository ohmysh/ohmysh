#!/bin/bash

# The Plugin support ZSH

#COLOR=(${fg[*]})
#BGCOLOR=(${bg[*]})
#COLOR[reset]="$reset_color"

cp "$OMS_DIR/usr/plugin/zsh-support/runtime.sh" "$OMS_CACHE/runtime-script/plugin-zsh-support.sh"
